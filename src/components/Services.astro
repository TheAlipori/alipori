---
import LandingIcon from "../icons/landingIcon.astro";
import CelIcon from "../icons/celIcon.astro";
import BrandIcon from "../icons/BrandIcon.astro";
import { getLangFromUrl, useTranslations } from "../i18n/utils";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<section id="services" class="scroll-mt-16">
  <div
    class="px-4 py-16 mx-auto sm:max-w-xl md:max-w-full lg:max-w-screen-xl md:px-24 lg:px-8 lg:py-20"
  >
    <div
      class="text-center max-w-2xl mx-auto mb-16"
      data-aos="fade-down"
      data-aos-duration="1000"
      data-aos-easing="ease-out-cubic"
    >
      <p class="text-lg" style="color: var(--text-body);">
        {t("services2")}
      </p>
      <h2 class="text-white text-4xl md:text-6xl font-bold mb-4">
        {t("services1")}
      </h2>
    </div>

    <div
      class="grid gap-8 lg:grid-cols-3"
      data-aos="fade-up"
      data-aos-duration="1000"
      data-aos-easing="ease-out-cubic"
    >
      <!-- Website Development Card -->
      <div
        class="relative group rounded-xl p-8 hover:shadow-lg transition-all duration-300 flex flex-col backdrop-blur-xl"
        style="background: linear-gradient(140deg, rgba(175,190,255,0.05), rgba(175,190,255,0.20));"
      >
        <LandingIcon class="w-16 h-16 mb-6 fill-white" />
        <h3 class="text-2xl font-bold mb-4 text-white">{t("services3")}</h3>

        <div class="my-6">
          <span class="text-4xl font-bold text-white">{t("services4")}</span>
          <span style="color: var(--text-body);">{t("services5")}</span>
        </div>

        <ul class="space-y-3 mb-8" style="color: var(--text-body);">
          <li class="flex items-center gap-3">
            <span class="text-white text-xl">✓</span>
            {t("services6")}
          </li>
          <li class="flex items-center gap-3">
            <span class="text-white text-xl">✓</span>
            {t("services7")}
          </li>
          <li class="flex items-center gap-3">
            <span class="text-white text-xl">✓</span>
            {t("services8")}
          </li>
        </ul>

        <a
          href="#contact"
          class="inline-block w-full py-3 text-center font-medium bg-white hover:bg-white/90 text-gray-900 rounded-lg transition-colors mt-auto"
        >
          {t("services9")}
        </a>
      </div>

      <!-- UI/UX Design Card -->
      <div
        class="relative group rounded-xl p-8 hover:shadow-lg transition-all duration-300 flex flex-col backdrop-blur-xl"
        style="background: linear-gradient(140deg, rgba(175,190,255,0.05), rgba(175,190,255,0.20));"
      >
        <CelIcon class="w-16 h-16 mb-6 fill-white" />
        <h3 class="text-2xl font-bold mb-4 text-white">{t("services10")}</h3>

        <div class="my-6">
          <span class="text-4xl font-bold text-white">{t("services11")}</span>
          <span style="color: var(--text-body);">{t("services12")}</span>
        </div>

        <ul class="space-y-3 mb-8" style="color: var(--text-body);">
          <li class="flex items-center gap-3">
            <span class="text-white text-xl">✓</span>
            {t("services13")}
          </li>
          <li class="flex items-center gap-3">
            <span class="text-white text-xl">✓</span>
            {t("services14")}
          </li>
          <li class="flex items-center gap-3">
            <span class="text-white text-xl">✓</span>
            {t("services15")}
          </li>
        </ul>

        <a
          href="#contact"
          class="inline-block w-full py-3 text-center font-medium bg-white hover:bg-white/90 text-gray-900 rounded-lg transition-colors mt-auto"
        >
          {t("services16")}
        </a>
      </div>

      <!-- Branding Card -->
      <div
        class="relative group rounded-xl p-8 hover:shadow-lg transition-all duration-300 flex flex-col backdrop-blur-xl"
        style="background: linear-gradient(140deg, rgba(175,190,255,0.05), rgba(175,190,255,0.20));"
      >
        <BrandIcon class="w-16 h-16 mb-6 fill-white" />
        <h3 class="text-2xl font-bold mb-4 text-white">{t("services17")}</h3>

        <div class="my-6">
          <span class="text-4xl font-bold text-white">{t("services18")}</span>
          <span style="color: var(--text-body);"> {t("services19")}</span>
        </div>

        <ul class="space-y-3 mb-8" style="color: var(--text-body);">
          <li class="flex items-center gap-3">
            <span class="text-white text-xl">✓</span>
            {t("services20")}
          </li>
          <li class="flex items-center gap-3">
            <span class="text-white text-xl">✓</span>
            {t("services21")}
          </li>
          <li class="flex items-center gap-3">
            <span class="text-white text-xl">✓</span>
            {t("services22")}
          </li>
        </ul>

        <a
          href="#contact"
          class="inline-block w-full py-3 text-center font-medium bg-white hover:bg-white/90 text-gray-900 rounded-lg transition-colors mt-auto"
        >
          {t("services23")}
        </a>
      </div>
    </div>

    <div class="mt-16 text-center">
      <p style="color: var(--text-body);">
        {t("services24")}
        <a href="#contact" class="text-white hover:text-white/80 underline">
          {t("services25")}
        </a>
      </p>
    </div>
  </div>
</section>
